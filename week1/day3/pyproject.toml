[project]
name = "day3"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "groq>=1.5.0",
    "python-dotenv>=1.2.2",
]

[tool.uv.workspace]
members = [
    "venv",
]
